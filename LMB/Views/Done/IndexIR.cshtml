@model IEnumerable<LMB.Models.ValueCheckList>

@{
    ViewBag.Title = "IndexIR";
}

<h2>Questions</h2>

<p>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdInspection)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.RowIDQuestion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.IdInspection)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RowIDQuestion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdValueCheckList }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdValueCheckList })
        </td>
    </tr>
}

</table>
